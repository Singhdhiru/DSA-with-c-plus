#include<iostream>
using namespace std;
int main(){

    // * Address-of operator->> if we want to access the address of memory
    int x = 10;
    int y = 15 ;
    cout<<"address of x is->>"<<&x<<endl;
    cout<<"address of y is->>"<<&y<<endl;
}